# MVP 状态与边界

本次公开的是 bd32a80d2ffa MVP 的产品源码及安装入口，运行代码未改。许可、公开说明和清单为发布新增内容。

## 已有验证

- 对应开发源码的 556 项自动测试通过，类型、UTF-8、交接资产与源码稳定性检查通过。
- 真实 SillyTavern / Tavern Helper 中验证固定与折叠双视图的六页自动更新、交易编辑、地图 JSON 导入、秘密过滤、分层与拖动、桌面与390px窄屏。
- 中文路径的缓存离线安装通过，含原生 LanceDB 与 Agent CLI 检查。
- 前序未变更模块已有真实酒馆配对/重连、恢复，以及 Agent 原生子代理的运行证据。

上述原始证据留在维护者的开发环境，未随本仓库公开。这里是验证摘要，不是公共CI的重跑结果。

## 未完成及已知限制

- 尚未实现向量量化检索；自然遗忘采用分层文本访问控制。再激活仍依赖逐字来源线索，语义再激活和分层恢复属于后续工作。
- 外围细节的30/60/180天期限是工程默认值，缺少多周/月真实使用校准。
- 100轮长对话为混合版本，含失败与修复，不是当前固定版本100轮全部通过。
- 地图和生理的真实模型抽取质量、角色长期表现、Agent到期主动投递仍有待验证项目。
- 原生群聊、续写、滑动/原生重生成不支持；单卡多NPC和面板受控重生成是独立能力。
- 首次纯联网冷安装曾遇到网络失败；已验证的是缓存离线安装及恢复。模型服务网络、限流和输出格式仍可能使某个处理阶段失败。
- 后台独立状态变化使用15秒空闲轮询；现实时间使用1秒定时器，但浏览器调度可能延迟。不能保证每轮一分钟内首字。

地图、推理模型、画像和主动性均需按使用说明配置；XLDB不提供LLM推理服务。
